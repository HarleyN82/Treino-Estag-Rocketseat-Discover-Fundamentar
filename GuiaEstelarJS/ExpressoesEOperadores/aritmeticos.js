let number = 20
let number2 = 2

// Multiplicação é o * (asterisco)
console.log(number * number2);

// Divisão é a / (barra)
console.log(number2/number);

// Soma é o + (positivo)
console.log(number + number2);

// Subtração é o - (negativo)
console.log(number - number2);

/* resto da divisão: sinal % */
let remainder
remainder = 11 % 9
console.log(remainder)

/* incremento: sinal ++ */
let increment = 0
console.log(++increment)
console.log(increment)

/* decremento: sinal -- */
let decrement = 0
decrement--
console.log(decrement)

/* exponencial: sinal ** */
console.log(2 ** 3)