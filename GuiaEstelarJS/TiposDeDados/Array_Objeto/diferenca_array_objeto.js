// [] = array, lista
// dados do mesmo tipo (não é regra)

var arr = [100,200,300,400,500];
console.log(arr);
console.log(typeof arr); // JS não diferencia entre objec e array
console.log(arr.length);
console.log(arr[0]);

// {} = objeto, dicionário
// chaves e valores

var obj = {
    nome: "Laurel",
    idade: 18
};

console.log(obj.nome);
console.log(obj.idade);